@namespace("ch.admin.bit.jeap.processarchive.test.decree.v2")
protocol DecreeProtocol {

  import idl "ch.admin.bit.jeap.processarchive.test.DecreeReference.avdl";

  record Decree {
    union{null, ch.admin.bit.jeap.processarchive.test.DecreeReference} decreeReference = null;
    string payload;
  }
}
