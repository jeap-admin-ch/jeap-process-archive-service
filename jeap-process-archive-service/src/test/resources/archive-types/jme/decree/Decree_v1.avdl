@namespace("ch.admin.bit.jeap.processarchive.test.decree.v3")
protocol DecreeProtocol {

  record DecreeReference {
    string type;
    string id;
  }

  record Decree {
    DecreeReference decreeReference;
    timestamp_ms createdAt;
    string title;
    string payload;
  }
}
